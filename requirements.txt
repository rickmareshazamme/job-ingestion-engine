# Core
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# Database
sqlalchemy[asyncio]
asyncpg
alembic
psycopg2-binary

# Task Queue
celery[redis]
redis

# HTTP
aiohttp
httpx

# HTML Parsing
beautifulsoup4
lxml

# Browser Automation
playwright

# MCP Server (requires Python 3.10+)
mcp

# Templates
jinja2

# Utilities
python-dotenv

# Google Indexing API (optional — only used if GOOGLE_SA_FILE is set)
google-auth

# AI Resume Match (Anthropic Claude) — optional, no-op without ANTHROPIC_API_KEY
anthropic

# PDF/DOCX parsing for resume upload
pypdf
python-docx

# Form-multipart parsing (FastAPI's File/UploadFile)
python-multipart

# Email (Job Alerts) — Resend HTTP API; no SDK, plain httpx call

# Itsdangerous — signed tokens for unsubscribe / alert confirmation
itsdangerous

# Testing
pytest
pytest-asyncio
